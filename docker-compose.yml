services:
  dev:
    build: .
    volumes:
      - .:/workspace
      - gradle-cache:/root/.gradle
    working_dir: /workspace
    command: sleep infinity
    environment:
      - GRADLE_USER_HOME=/root/.gradle

volumes:
  gradle-cache:
